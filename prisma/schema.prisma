generator client {
     provider = "prisma-client-js"
   }

datasource db {
  provider = "postgresql"
}


model ResumeSection {
  id        Int      @id @default(autoincrement())
  resumeId String
  section  String
  content  String
  
  @@map("resume_sections")
}